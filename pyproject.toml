[tool.poetry]
name = "vk-bot"
version = "0.1.0"
description = "VK Bot for country statistics"
authors = ["Egor Stolbov"]

[tool.poetry.dependencies]
python = "^3.9"
vk-api = "^11.9.9"
matplotlib = "^3.7.0"
pillow = "^10.0.0"
seaborn = "^0.12.2"
pandas = "^2.0.0"
numpy = "^1.24.0"
sqlalchemy = "^2.0.44"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
